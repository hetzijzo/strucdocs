language: java
sudo: required
jdk:
  - oraclejdk8
services:
  - docker
before_install:
  - chmod +x gradlew
script:
  - ./gradlew clean build
branches:
  only:
    - master
    - develop
    - /^feature.*$/
    - /^hotfix.*$/
notifications:
  email: false
